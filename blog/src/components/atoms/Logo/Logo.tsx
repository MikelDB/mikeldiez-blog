import React from 'react'
import { Link } from 'gatsby'


const Logo: React.FC = ({}): React.ReactElement => {
    return (
        <Link to="/" title="Logo">
            MikelDB
        </Link>
    );
};


export default Logo;